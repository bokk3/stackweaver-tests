# Stackweaver tests

Stackweaver aims to be fully tfe compliant, meaning the tfe provider can be used just as in terraform entreprise.

## Workspace

Set the `TFE_TOKEN` and use [providers]

```bash
export TFE_TOKEN=""
```

```pwsh
$env:TFE_TOKEN=""
```